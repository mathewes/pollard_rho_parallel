default(realprecision,38);
do(p) = {
  my(v = [-3,3,2,0,1/2+I]);
  my(znf = zetakinit(p));
  for(i=1,#v, print(zetak(znf,v[i])));
}

allocatemem(20*10^6);
do(y^2-3);
do(y^2+3);
do(y^3-4*y+2);
do(y^3-2);
do(y^5-3*y^2+y+2);
